
class Computer {
    // Propriétés - attribut qui vont servir à décrire les objets
    var cpu:String
    var gpu:String
    var ram:Int
    
    init(cpu:cpu, gpu:gpu, ram:ram) {
        self.cpu = cpu
        self.gpu = gpu
        self.ram = ram
    }
}
